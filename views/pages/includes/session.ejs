<% if(sessionUser !== undefined){ %>
<nav>
	<div class="dropdown-toggle userAccountInBlock"></div>
	<ul class="dropdownsession">
		<li>
			<a href="/profile" title="<%= locale.profile %>">Профиль</a>
		</li>
		<% if(sessionUser !== undefined){ %>
		<li>
			<a href="/index-panel" title="<%= locale.profile %>">Панель управления</a>
		</li>
		<% } %>
		<li>
			<a href="/logout" title="<%= locale.profile %>">Выход</a>
		</li>
	</ul>
</nav>	
<% } else { %>
<a href="/login" title="<%= locale.signin %>"><div class="logInBlock"></div></a>
<% } %>